/*global $:true, console:true */
$(function() {
    // Slider setup
     $('.flexslider').flexslider({
         slideshow: true,
         directionNav: true,
         pauseOnHover: true,
         slideshowSpeed: 5000
     });
     
     // Leaflet setup
     
});
